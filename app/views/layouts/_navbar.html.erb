<nav class="navbar navbar-expand-lg navbar-light bg-light shadow rounded-bottom fixed-top" style="background-color: #e3f2fd;">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">かなこれ杯</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/scores">対局結果</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/grand_slums">役満達成者</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/participants">参加者</a>
        </li>
        <% if logged_in? then %>
        <li class="nav-item">
          <%= link_to "管理者モードを終了する", logout_path, data:{ turbo_method: :delete }, class: "nav-link" %>
        </li>
        <% end %>
      </ul>
    </div>
    <!--
    <div class="d-flex">
      <% if logged_in? then %>
        <div><%= link_to "一般モードに切り替える", logout_path, data:{ turbo_method: :delete }, class: "btn btn-outline-success" %></div>
      <% else %>
        <div><%= link_to "管理者モードに切り替える", login_path, class: "btn btn-outline-success" %></div>
      <% end %>
    </div>
    -->
  </div>
</nav>
